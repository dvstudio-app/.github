# .github

Default community health files for DV Studio's public repositories. GitHub applies them to
every repository in this organization that has no file of its own: the security policy
(`SECURITY.md`), the support page (`SUPPORT.md`) and the issue templates
(`.github/ISSUE_TEMPLATE/`).
